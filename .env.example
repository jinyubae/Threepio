# Copy this file to .env and fill in whichever providers you want to use.
# You only need to set at least one key — providers without keys will be hidden in the UI.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
